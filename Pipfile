[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pygame = ">=2.5.0"
numpy = ">=1.24.0"
gymnasium = ">=0.29.0"
stable-baselines3 = ">=2.0.0"
torch = ">=2.0.0"
tensorboard = ">=2.14.0"
tqdm = ">=4.65.0"
rich = ">=13.0.0"

[dev-packages]

[requires]
python_version = "3.11"
